---

command:
  'php -r "phpinfo();"':
    exit-status: 0
    stdout:
      - "memory_limit => -1 => -1"
      - "ZIP-based phar archives => enabled"
      - "gzip compression => enabled"
      - "Zip => enabled"
